<h2>Switch</h2>
<!-- <let/isChecked=false/> -->
<!-- onCheckedChange({ checked }) { isChecked = checked; }
checked=isChecked -->

<id/switchId/>

<app-switch/api id=switchId>
  Switch
</app-switch>

<pre>${JSON.stringify(
  {
    isChecked: api.isChecked,
    isDisabled: api.isDisabled,
    isFocused: api.isFocused,
  },
  null,
  2
)}</pre>
